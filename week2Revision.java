public class week2Revision {

    public static void main(String[] args) {
        // ! Class - Attributes (private), Constructors, Methods
        // practice class + loop
        // Method - getter, setter 
        // Method - presentation (isVip, isAdult, subTotal)
        // one-to-one: Class has Class
        // One-to-many: class has array

        // examples: customer/order/item, person

        // BigDecimal (BigDecimal.valueOf()), BigDecimal + - * / , String Methods, Array + loop ( String[], int[])
        // equals, compareTo
        //Wrapper Class


        // LocalDate  (LocalDate.of())
    }
    
}
